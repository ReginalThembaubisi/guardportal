# Left unset on purpose: the app calls the API via relative paths
# (/api/...), proxied to the backend by vite.config.ts's dev server proxy.
# Only set VITE_API_BASE_URL if the backend genuinely lives on a different
# origin than this app (e.g. a real non-local deployment).
